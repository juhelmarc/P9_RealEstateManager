<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openclassrooms.realestatemanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCyn3_Hvu0b4PlANLre07Wvme5VCR4qewo">

        </meta-data>
<!--        android:value="${MAPS_API_KEY}"-->
        <activity
            android:name="com.openclassrooms.realestatemanager.ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.openclassrooms.realestatemanager.ui.detail.DetailActivity" />
        <activity android:name=".ui.detailslider.DetailSliderActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".ui.addProperty.AddOrUpdatePropertyActivity"
            />

    </application>

</manifest>